#include<iostream>
#incldude<cstring>

using namespace std;
long long int dp[17005][17005];
int main(){
    int t;
    cin >> t;
    while(t--){
        memset(dp,0,sizeof(dp));
        int n;
        cin >> n;
        int a[n];
        for(int i=0;i<n;i++) cin >> a[i];

        for(int i=0;i<)
    }
}